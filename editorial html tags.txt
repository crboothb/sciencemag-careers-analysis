# useful tags for editorial scraping

<h2 class="media__headline">
	<a href="URLtoArticle>Title title title</a>
<h2>

<div class="media__deck">teaser line i.e. Documents like these can help labs run more smoothly</div>

make the shell
scrapy shell "https://www.sciencemag.org/careers/articles?page=1"

commands in shell:

block = response.css("div.media__body")[0]

text = block.css("a::text").get()
#byline = block.css("p.byline").get() # can't figure this one out because "by" is in there as a string
preview = block.css("div.media__deck::text").get()

